type Query {
  job(id: ID!): Job
  jobs: [Job]
  companies: [Company]
}

#  `!` means that the field is always not-null
type Job {
  id: ID!
  title: String
  description: String
  company: Company
}

type Company {
  id: ID!
  name: String
  description: String
  jobs: [Job]
}
